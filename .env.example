
# --- Affiliate attribution loop ---
AFFILIATE_HMAC_SECRET=dev-shared-secret-change-me
MOCK_BOOK_URL=http://localhost:4100
MOCK_AFFILIATE_ID=mm-mock-01
AFFILIATE_DEV_REGION=BR
ATTRIBUTION_WINDOW_DAYS=30

# --- Password reset email (Nodemailer SMTP) ---
# If SMTP_* are unset, the reset link is LOGGED to the server console instead of emailed.
# Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=you@gmail.com, SMTP_PASS=<app password>.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Hit Confirmed <no-reply@hitconfirmed.local>
APP_BASE_URL=http://localhost:5000
RESET_TOKEN_TTL_MINUTES=60
